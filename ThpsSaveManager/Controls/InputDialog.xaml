<controls:AdonisWindow  xmlns:controls="clr-namespace:AdonisUI.Controls;assembly=AdonisUI"
        x:Class="ThpsSaveManager.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ThpsSaveManager"
        mc:Ignorable="d"
        Title="PLACEHOLDER TITLE" Width="400" Height="200" SizeToContent="WidthAndHeight" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="PLACEHOLDER TEXT" x:Name="txtDescription" Margin="5" />
        <TextBox Grid.Row="1" x:Name="txtInput" Margin="5" KeyUp="txtInput_KeyUp" />
        <TextBlock Grid.Row="2" x:Name="txtError" Text="" Foreground="Red" Margin="5" />
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" x:Name="btnOk" Width="100" Margin="5" Click="OK_Click"/>
            <Button Content="Cancel" x:Name="btnCancel" Width="100" Margin="0,5,5,5" Click="Cancel_Click" />
        </StackPanel>
    </Grid>
</controls:AdonisWindow>
